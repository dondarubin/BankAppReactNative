import {AppColors} from "../../constants";

export const BOX_SHADOW = {
  shadowColor: AppColors.MAIN_HOVER,
  shadowOffset: {
    width: 0,
    height: 2,
  },
  shadowOpacity: 0.15,
  shadowRadius: 10,
  elevation: 5,
}