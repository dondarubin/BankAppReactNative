import {FC} from "react";
import {IAccount} from "../types";
import {ImageBackground, View} from "react-native";


const ImageCard: FC<{ account: IAccount }> = ({account}) => {

  return (
    <></>
  )
}

export default ImageCard